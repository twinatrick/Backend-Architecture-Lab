# 🚀 後端微服務架構：未來優化方向指引

本文件詳盡記錄了系統在高併發壓力測試中暴露的深層架構問題、物理機制分析，並指引未來的優化方向與演進道路。

---

## 一、動態查詢 (Search API) 的快取與防穿透排隊衝突 🚨

### 1. 物理現象回顧
在 500 併發壓力測試中，系統呈現了極具反差的數據表現：
* **有快取模式**：`Project` 服務平均延遲高達 **4.4 秒**，吞吐量僅 **10.3 req/s**，且伴隨 **21.8% ~ 53.6%** 的 500 超時錯誤率。
* **無快取模式**：`Project` 服務平均延遲僅 **3.3 秒**，吞吐量狂飆至 **37.5 req/s**，錯誤率為完美的 **0%**。

### 2. 深層物理機制剖析
此現象並非虛擬執行緒（Virtual Threads）調度不力，而是由於**系統安全防禦機制與動態業務查詢之間的物理衝突**：
* **100% 快取穿透 (Cache Miss)**：
  `searchProjects` 與 `search` 等 API 標註了 `@Cacheable(sync = true)`，其 Key 依賴於動態查詢條件 `key = "'search:' + #query.toString()"`。在高併發下，查詢條件高度動態且隨機，導致 Key 幾乎 **100% 不命中**。
* **Semaphore(10, true) 造成的排隊延遲累積**：
  為防止快取穿透擊穿資料庫，我們在 `CachePenetrationProtectionCache` 中設計了併發限流的 `Semaphore(10, true)`。當 500 個執行緒因 Cache Miss 湧入時，**490 個執行緒被強行掛起排隊**。這 490 個執行緒在 JVM 內的排隊時間被 JMeter 100% 統計為回應延遲。
* **30 秒排隊超時熔斷 (500 錯誤的根源)**：
  500 個執行緒被分成了 50 批（每批 10 個）依次執行。即使資料庫分頁查詢極快（如每批只需 600ms），排名後面的批次其等待時間也必將突破 **30 秒**（50 * 600ms = 30s）。一旦突破 30 秒，執行緒在 `concurrencySemaphore` 中獲取信號量超時，直接拋出 `"系統繁忙，請稍候再試"`（500 錯誤）。
* **無快取時直通車優勢**：
  當關閉快取時，`CachePenetrationProtectionCache` 被完全跳過，無任何 `Semaphore` 排隊限制。此時已被優化過的大表（剔除 findAll）在分頁查詢下執行極快，連線池複用率極高，因此 500 個請求直接暢通返回，錯誤率為 0%。

---

## 二、未來優化方案 🛠️

針對此衝突，我們規劃了以下三種優化路徑，在未來的架構演進中，應優先採納 **方案 A**。

### 🌟 方案 A：移除非單筆（動態 Search）API 的快取標註（強烈推薦）
* **設計理念**：**動態分頁、複雜篩選的 Search 介面本質上不應該被快取，更不應該設置併發同步排隊鎖（`sync = true`）**。快取與併發同步鎖應只服務於「Key 靜態確定、高頻、單筆、極具穿透風險」的熱點數據。
* **具體做法**：
  1. 移除非單筆查詢、分頁篩選等動態 Search 介面（如 `searchProjects`、`searchCurrentUserProjectsCache`、`search`、`currentUserSkills`、`levelsearch`）上的 `@Cacheable` 標註。
  2. 讓這類動態分頁查詢直接直通資料庫（確保資料庫對應欄位設有正確的 B-Tree 索引）。
  3. 繼續保留單筆熱點查詢（如 `/get/{id}`、`getProjectListCache`、`getProjectSkillsCache`）之快取與 `TransactionExecutor` 唯讀事務包裝。
* **預期效果**：
  * **有快取模式下的錯誤率將瞬間降至 0%**。
  * Search 查詢的吞吐量將與無快取直通車一樣高（約 40 req/s），同時核心單筆熱點繼續享有 100% 的 Redis 安全防火牆。

### 方案 B：動態/條件式放寬 Semaphore 限流併發度
* **設計理念**：調高快取層對大併發的物理承載上限，縮短隊列排隊長度。
* **具體做法**：
  1. 在 `CachePenetrationProtectionCache` 中，將 `concurrencySemaphore` 的最大併發度由 `10` 調整為 `50` 或 `100`。
  2. 或者，根據緩存名稱（Cache Name）實施差異化限流：對於 `projects`、`skills` 等大表查詢，給予較大的 Semaphore 額度（如 100）；對於單筆 `projectSkills`則維持較小額度（如 10）。
* **預期效果**：大併發下的排隊等待時間大幅降低，30 秒超時的熔斷錯誤率將大幅下降。

### 方案 C：將 Search 類快取的 `sync` 屬性設為 `false`
* **設計理念**：允許動態 Search API 在快取不命中時，以非同步（非併發阻塞）的形式加載資料庫，移除鎖等待。
* **具體做法**：
  1. 保留 `@Cacheable` 快取以減少資料庫負載，但將 `sync = true` 改為 `sync = false`。
  2. 此時快取不命中時，所有執行緒會直接穿透去讀資料庫，不再進入 `CachePenetrationProtectionCache` 的 Redisson 分散式鎖與 `Thread.sleep` 輪詢邏輯。
* **預期效果**：錯誤率降至 0%，吞吐量大幅回升，但在極端高併發快取失效瞬間會對資料庫產生穿透壓力。

---

## 三、虛擬執行緒（Virtual Threads）於併發系統中的局限性與設計考量 🧠

在 Java 21+ 環境下，虛擬執行緒雖然能極大釋放 platform 執行緒的調度壓力，但在設計高併發鎖機制時，仍需謹記以下原則：
1. **虛擬執行緒不是併發臨界資源爭搶的銀彈**：虛擬執行緒能優化 I/O 阻塞，但一旦 500 個虛擬執行緒排隊爭搶同一個臨界鎖（如 Redisson 鎖、Semaphore 信號量），其排隊延遲（Queueing Delay）依然會硬生生累加在客戶端。
2. **防範 Carrier Thread Pinning（執行緒釘死）**：在高併發同步鎖內部，嚴禁調用任何涉及 `synchronized` 區塊（如部分舊版 JDBC、Hibernate、Redis 驅動底層）的慢速 I/O 動作，避免底層 Carrier 執行緒被釘死導致全系統調度飢餓。
3. **主動壓力轉移的代價**：分散式鎖與信號量（如 `Semaphore`）的設計初衷，是**主動將「系統崩潰壓力」轉化為「客戶端排隊等待時間」**。這是一個在「安全」與「極限效能」之間的權衡。優良的架構應只保護核心單筆熱點，而非對動態慢查詢進行大面積限流排隊。
